database:
  type: sqlite
  database: dbname

  # Ignore if using SQLite
  host: localhost
  port: 3306
  user: admin
  password: password

SignIdentifier: [tshop]

MaxStoredGuisPerPlayer: 50

MaxPagesPerGui: 50

# No touch please
ConfigVersion: ${project.version}